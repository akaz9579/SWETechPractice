function add(a,b){
    return a+b;
}

const addA = (a,b) => a+b;

//same shi

//when the output is more lines, use curly braces 


const add3 = (a,b) => {
    console.log(`add${a} and ${b}`);
    let sum = a +b;
    return sum;

}